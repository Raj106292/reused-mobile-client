import React from 'react';
import Loader from '../../Common/Loader';

const HomePage = () => {
    return (
        <div>
            <Loader></Loader>
        </div>
    );
};

export default HomePage;